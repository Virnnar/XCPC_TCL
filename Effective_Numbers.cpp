#include "Monday.h"

using namespace virmar;

void init();

void doit() {
    ll l, r; cin >> l >> r;
    for (int i = )
}

signed main() {
    virmar::READ_ARC = false;
    init();
    int T = 1;// cin >> T;
    while (T --> 0) doit();
    return 0;
}

// 1 4 9 16 
//2 6 
void init() {

}