#include "Monday.h"

using namespace virmar;

void init();

void doit() {
    int x; cin >> x;
    int a, b, c, ans = 0;
    a = b = c = 0;


    // while (min({a, b, c}) < x) done(a, b, c), ;
}

signed main() {
    virmar::READ_ARC = false;
    init();
    int T = 1; //cin >> T;
    while (T--) doit();
    return 0;
}

void init() {

}